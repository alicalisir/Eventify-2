name: context_aware_lints
description: Custom lint rules — spacing grid and typography enforcement
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  custom_lint_builder: ^0.8.0
  analyzer: '>=8.0.0 <14.0.0'
